# Backend Anti-Patterns

1. 反模式：在 handler 中堆业务逻辑
- 为什么错：耦合高，难测，复用差。
- 替代方式：handler 仅做协议转换，业务编排放 `internal/`。

2. 反模式：跳过错误语义统一
- 为什么错：前后端难以稳定处理失败路径。
- 替代方式：统一错误模型并在 spec 中定义。

3. 反模式：先改数据库后补 schema 文档
- 为什么错：数据演进不可追踪。
- 替代方式：先更新 `spec/data/schema.md`，再做迁移。
