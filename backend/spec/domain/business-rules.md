# Business Rules (Backend)

## 核心规则
1. 管理员只有一个，初始账号由系统初始化脚本创建。
2. 新用户注册后状态固定为 `pending`。
3. `pending` 用户不可登录。
4. 仅管理员可审批用户，审批后状态转为 `approved`。
5. 仅 `approved` 用户可访问首页业务数据。

## 默认管理员规则
- 默认管理员用户名：`admin`
- 默认管理员密码：`Admin@123456`
- 约束：上线前必须改密（本期作为开发默认值）
