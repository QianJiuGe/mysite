# AI Workflows (Backend)

## 任务模板
- 输入：需求片段 + 受影响 spec
- 约束：业务规则、错误模型、数据约束
- 完成定义：测试用例覆盖 + spec 同步 + notes 更新

## 角色协作
1. Tester 先补 `spec/testing/test-cases.md`。
2. Implementer 再改代码（最小可行）。
3. Reviewer 最后审边界、风险、回滚。
