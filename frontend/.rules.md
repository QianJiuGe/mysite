# Frontend Rules

1. 页面职责以 `spec/pages.md` 为准。
2. 视觉约束以 `spec/design-tokens.md` 为准。
3. 新交互必须包含可验证测试场景。
4. 组件优先单一职责，避免巨型组件。
5. 禁止硬编码颜色、间距、字号等魔法值。
6. 状态管理方案必须可追踪与可测试。
7. 错误与空状态必须显式处理。
8. 可访问性是默认要求，不是可选项。
9. 响应式兼容移动端与桌面端。
10. 一次变更聚焦单目标，避免无关重构。
11. 文档与实现不一致时先修文档。
12. 未验证不得声称完成。
